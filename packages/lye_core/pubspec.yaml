name: lye_core
description: Log Your Event (LYE) core - verifiable, hash-chained event model, CSV codec, redaction, recorder and stores. Pure Dart, no Flutter, no Serverpod.
version: 0.1.0
publish_to: none
repository: https://github.com/CristiPerciun/log-your-event

environment:
  sdk: '>=3.12.0 <4.0.0'

dependencies:
  crypto: ^3.0.7
  meta: ^1.18.0

dev_dependencies:
  lints: ^6.0.0
  test: ^1.31.0
