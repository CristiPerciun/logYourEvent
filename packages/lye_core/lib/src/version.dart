/// Version of the library, written into every manifest as `generator`.
///
/// Must equal the `version` field of every `pubspec.yaml` in the repository
/// and the Git tag of the release (`tool/check_versions.dart` enforces it).
const String lyeVersion = '0.1.0';
